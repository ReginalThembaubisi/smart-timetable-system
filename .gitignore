vendor/
node_modules/
**/dist/
**/build/
logs/
logs/*.log
logs/*.txt
logs/archive/
frontend/
archive/
docs/
# composer.lock - Tracked for deterministic builds on Railway
!composer.lock
package-lock.json
public.code-workspace
.DS_Store
Thumbs.db
*.env
*.local

# Sensitive configuration files (if they contain hardcoded credentials)
# Uncomment if config.php has sensitive data:
# admin/config.php

# Database dumps with real data
*.sql
!database_setup.sql
!create_work_experience_table.sql

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Flutter artifacts
smart_timetable_application/.dart_tool/
smart_timetable_application/.flutter-plugins
smart_timetable_application/.flutter-plugins-dependencies
smart_timetable_application/pubspec.lock
smart_timetable_application/build/
smart_timetable_application/.packages
smart_timetable_application/generated/

# Helper files for GitHub upload (not needed in repo)
BEFORE_GITHUB_CHECKLIST.md
QUICK_GITHUB_UPLOAD.md
UPLOAD_TO_GITHUB.bat
GITHUB_README.md



[phases.setup]
nixPkgs = ["bash", "git", "curl", "unzip", "php82"]

# Skip install phase - build script handles Flutter setup and pub get
[phases.install]
cmds = []

[phases.build]
cmds = ["bash build_railway.sh"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t build/web"

